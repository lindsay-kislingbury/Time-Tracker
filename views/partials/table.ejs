<!--Table-->
<div class="container px-0 mx-0" id="table">
    <div class="table-responsive-md w-100 h-100">
        <div class="container px-0" id="stamps">
            <table class="table table-borderless rounded">
                <thead class="sticky-top text-bg-dark py-0">
                    <tr>
                    <th colspan="3" class="text-center">Recent Entries</th>
                    </tr>
                </thead>
                <tbody class="table-borderless" style="background-color: rgba(255, 255, 255, 0.3);">
                    <%if(count<=0){%>
                        <td colspan="4">
                            <p>No Timestamps Yet!</p>
                        </td>
                    <%}else{%>
                        <%if(count>10){%>
                            <%count=10;%>
                        <%}%>
                        <% for (var i=count-1; i>=0; i--){%>
                            <tr class="bg-transparent text-dark">
                                <td class="col-3 py-0 my-0">
                                    <span class="text-capitalize fw-bold">
                                        <%-timestamps[i].title%>
                                    </span>
                                    <br>
                                    <span>
                                        <%-timestamps[i].date.toLocaleDateString('es-pa');%>
                                    </span>
                                </td>
                                <td class="col-2 px-0 py-0 text-start my-0 align-middle">
                                    <span class="fs-5">
                                        <%-new Date(timestamps[i].elapsedTime * 1000).toISOString().slice(11, 19);%>
                                    </span>
                                </td>
                                <td class="col-1 align-end">
                                    <button class="btn bg-transparent" id="button-dash-edit" onclick="openModal('<%-timestamps[i]._id%>')" data-bs-toggle="modal" data-bs-target="#editModal">
                                        <i class="fa-solid fa-ellipsis-vertical fa-lg"></i>
                                    </button>
                                </td>
                            </tr>
                            <tr> 
                                <td colspan="4" class="py-0 my-0">
                                    <% for (var j=0; j<timestamps[i].tags.length; j++){ %>
                                        <span class="badge badge-secondary rounded-pill"><%-timestamps[i].tags[j]%></span>
                                    <% } %>
                                    <hr class="border border-dark border-2 border-opacity-60 mt-1 mb-2">
                                </td>
                            </tr>
                        <%}%>
                    <%}%>
                </tbody>
            </table>
        </div>
    </div>
</div>

